# Flask configuration
SECRET_KEY=dev-key-change-in-production

# Database Configuration
# For local development, sqlite:///chess.db is default.
# For production (Railway), use mysql+pymysql://user:pass@host:port/dbname.
DATABASE_URL=sqlite:///chess.db
